# Task 1 — Baseline Coverage

Instructions and artifacts for Task 1 (baseline coverage).

- Place baseline coverage HTML output at `reports/baseline_coverage/`.
- Run from `Ai model fyp`:

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements-dev.txt
pytest
```
